# RuYou Workspace Agent Navigation

This is the parent workspace for active work projects. Treat it as a multi-project container, not as a single repository.

## Safety Rules

- Work on one top-level project folder at a time.
- Before changes that could affect several projects, stop and confirm the scope with the user.
- Do not run broad formatting, dependency updates, mass renames, or cross-repository scripts unless explicitly requested.
- Prefer each repository's own `AGENTS.md` and `docs/` before inspecting implementation files.
- Do not use repository `README.md` files as the main AI-agent source of truth when `AGENTS.md` or `docs/` exist.

## Top-Level Project Folders

- `alpharm/` - Alpharm projects: `alpharm-admin`, `alpharm-mobile`, `alpharm-web`.
- `amazi/` - Amazi AI platform: `crm-ai-agent`.
- `eco-bike/` - Eco Bike projects: `eco-landing`, `eko-front`.
- `expert-teach-center/` - Expert Teach Center projects: `expert-teach-center-vebview`.
- `mincult/` - Mincult projects: `mincult-admin-2`, `mincult-frontend`, `mincult-mobile`.
- `music/` - Health music apps: `music-android`, `music-ios`.
- `on-fasion/` - On Fashion projects: `online-fashion.ru-bitrix`, `on-fashion-android`, `on-fashion-ios`.
- `panic-vpn/` - Panic VPN projects: `panic-vpn`.
- `podvijnik/` - Podvijnik projects: `podvijnik-frontend`, `podvizhnik-backend-php`.
- `premium/` - Premium projects: `premium-crm-web`, `premium_service_v2`.
- `privratnik/` - Privratnik projects: `lk`, `privratnik-frontend`.
- `ruyou/` - RuYou company projects: `ruyou_black`.
- `semeli/` - Semeli projects: `semeli-frontend`.
- `spektra-diagnostica/` - Spektra Diagnostica projects: `spektra-diagnostica-page`.
